import React,{useEffect,useState} from 'react';import {createRoot} from 'react-dom/client';import {BrowserRouter,Routes,Route} from 'react-router-dom';import {onAuthStateChanged} from 'firebase/auth';import {auth} from './lib/firebase';import Layout from './components/Layout';import Home from './pages/Home';import Menu from './pages/Menu';import About from './pages/About';import Contact from './pages/Contact';import Cart from './pages/Cart';import Login from './pages/Login';import Admin from './pages/Admin';import './styles.css';
function App(){const [cart,setCart]=useState(()=>JSON.parse(localStorage.getItem('royal-cart')||'{}'));const [user,setUser]=useState(null);useEffect(()=>{localStorage.setItem('royal-cart',JSON.stringify(cart))},[cart]);useEffect(()=>auth?onAuthStateChanged(auth,setUser):undefined,[]);const add=id=>setCart(c=>({...c,[id]:(c[id]||0)+1}));const inc=add;const dec=id=>setCart(c=>{const n={...c};if((n[id]||0)<=1)delete n[id];else n[id]--;return n});const clear=()=>setCart({});const count=Object.values(cart).reduce((a,b)=>a+b,0);return <Layout cartCount={count} user={user}><Routes><Route path="/" element={<Home cart={cart} onAdd={add} onInc={inc} onDec={dec}/>}/><Route path="/menu" element={<Menu cart={cart} onAdd={add} onInc={inc} onDec={dec}/>}/><Route path="/about" element={<About/>}/><Route path="/contact" element={<Contact/>}/><Route path="/cart" element={<Cart cart={cart} onInc={inc} onDec={dec} onClear={clear} user={user}/>}/><Route path="/login" element={<Login user={user}/>}/><Route path="/admin" element={<Admin user={user}/>}/></Routes></Layout>}
createRoot(document.getElementById('root')).render(<BrowserRouter><App/></BrowserRouter>);
