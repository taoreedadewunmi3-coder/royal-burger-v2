import {useState} from 'react';import {menu,categories} from '../data';import ProductCard from '../components/ProductCard';export default function MenuPage({cart,onAdd,onInc,onDec}){const [cat,setCat]=useState('All');const list=cat==='All'?menu:menu.filter(x=>x.category===cat);return <><div className="container pageHero"><div className="eyebrow">Choose your craving</div><h1>Royal Menu</h1><p className="muted">Fresh burgers, golden sides and cold drinks.</p><div className="filters">{categories.map(c=><button className={'filter '+(cat===c?'active':'')} onClick={()=>setCat(c)} key={c}>{c}</button>)}</div></div><section className="section" style={{paddingTop:20}}><div className="container"><div className="cards">{list.map(x=><ProductCard key={x.id} item={x} qty={cart[x.id]||0} onAdd={onAdd} onInc={onInc} onDec={onDec}/>)}</div></div></section></>}
