import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {auth,firebaseConfigured} from '../lib/firebase';import {signInWithEmailAndPassword,createUserWithEmailAndPassword,signOut} from 'firebase/auth';
export default function Login({user}){const [signup,setSignup]=useState(false),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const nav=useNavigate();async function submit(e){e.preventDefault();setError('');try{if(!firebaseConfigured)throw new Error('Firebase is not configured. Add your VITE_FIREBASE_* values.');signup?await createUserWithEmailAndPassword(auth,email,password):await signInWithEmailAndPassword(auth,email,password);nav('/')}catch(x){setError(x.message)}}if(user)return <div className="container login"><div className="loginbox"><h2>You’re signed in.</h2><p className="muted">{user.email}</p><button className="btn dark" onClick={()=>signOut(auth)}>Sign out</button></div></div>;return <div className="container login"><div className="loginbox"><div className="eyebrow">Your Royal account</div><h1>{signup?'Create account':'Welcome back'}</h1>{!firebaseConfigured&&<div className="notice">Firebase is ready in the codebase, but credentials must be added to <b>.env</b> before authentication can run.</div>}<form className="formgrid" onSubmit={submit}><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={6}/></div>{error&&<div className="notice">{error}</div>}<button className="btn primary">{signup?'Sign up':'Log in'}</button></form><p className="muted">{signup?'Already have an account?':'New here?'} <button style={{border:0,background:'none',fontWeight:800,cursor:'pointer'}} onClick={()=>setSignup(!signup)}>{signup?'Log in':'Create one'}</button></p><Link to="/">← Back home</Link></div></div>}
