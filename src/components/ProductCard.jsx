import {Plus,Minus,ShoppingBag} from 'lucide-react';import {money} from '../data';export default function ProductCard({item,qty=0,onAdd,onInc,onDec}){return <article className="card reveal"><div className="cardimg"><img src={item.image} alt={item.name}/>{item.tag&&<span className="tag">{item.tag}</span>}</div><div className="cardbody"><h3>{item.name}</h3><div className="price">{money(item.price)}</div><p>{item.description}</p><div className="cardfoot">{qty>0?<div className="qty"><button onClick={()=>onDec(item.id)}><Minus size={15}/></button><span>{qty}</span><button onClick={()=>onInc(item.id)}><Plus size={15}/></button></div>:<span/>}<button className="add" onClick={()=>onAdd(item.id)}><ShoppingBag size={15}/> Add</button></div></div></article>}
