import {useEffect,useRef} from 'react';
import * as THREE from 'three';
export default function Burger3D(){const ref=useRef(null);useEffect(()=>{const el=ref.current;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(35,1,.1,100);camera.position.set(0,1.1,7);const renderer=new THREE.WebGLRenderer({antialias:true,alpha:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(400,400,false);el.appendChild(renderer.domElement);const group=new THREE.Group();scene.add(group);const mat=(color,rough=.7)=>new THREE.MeshStandardMaterial({color,roughness:rough,metalness:.05});const bun=mat(0xd99038), patty=mat(0x4b2417), cheese=mat(0xffc400), green=mat(0x5b8d35), sauce=mat(0xd71920);function cyl(r,h,m,y){const o=new THREE.Mesh(new THREE.CylinderGeometry(r,r*.96,h,64),m);o.position.y=y;group.add(o)}cyl(1.75,.55,bun,1.55);cyl(1.72,.12,green,1.15);cyl(1.7,.32,cheese,.98);cyl(1.68,.45,patty,.62);cyl(1.72,.10,sauce,.36);cyl(1.68,.42,patty,.05);cyl(1.75,.18,green,-.25);cyl(1.8,.42,bun,-.52);for(let i=0;i<16;i++){const s=new THREE.Mesh(new THREE.SphereGeometry(.045,12,8),mat(0xffe7b0));const a=i*.75;s.position.set(Math.cos(a)*(1.25+Math.random()*.35),1.83+Math.random()*.12,Math.sin(a)*(1.25+Math.random()*.35));group.add(s)}const light=new THREE.HemisphereLight(0xffffff,0x8a5a32,2.2);scene.add(light);const key=new THREE.DirectionalLight(0xffffff,2.8);key.position.set(4,6,5);scene.add(key);let frame;const animate=()=>{group.rotation.y+=.006;group.rotation.x=Math.sin(Date.now()*.001)*.035;renderer.render(scene,camera);frame=requestAnimationFrame(animate)};animate();const resize=()=>{const w=Math.min(el.clientWidth||400,400);renderer.setSize(w,w,false);camera.aspect=1;camera.updateProjectionMatrix()};resize();window.addEventListener('resize',resize);return()=>{cancelAnimationFrame(frame);window.removeEventListener('resize',resize);renderer.dispose();el.removeChild(renderer.domElement)}},[]);return <div ref={ref} aria-label="3D rotating Royal Burger" style={{width:'100%',height:'100%',display:'grid',placeItems:'center'}}/>}
